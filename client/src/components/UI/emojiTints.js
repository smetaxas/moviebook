import clapperboardRed from '../../assets/clapperboard-red.svg'

// Emoji characters that should render as a custom-recolored asset
// instead of the default Twemoji artwork.
export const CUSTOM_EMOJI = {
  '🎬': clapperboardRed
}
